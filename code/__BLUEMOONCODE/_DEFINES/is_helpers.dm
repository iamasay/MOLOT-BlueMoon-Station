#define isxenmob(A) (istype(A, /mob/living/simple_animal/hostile/blackmesa/xen))

#define iscuratorjob(A) (_iscuratorjob(A))
// Big Manipulator - is_helpers
#define iscloset(A) (istype(A, /obj/structure/closet))
